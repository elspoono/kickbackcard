@css {@import url('//fonts.googleapis.com/css?family=Open+Sans');}
@css {@import url('//fonts.googleapis.com/css?family=Luckiest+Guy');}
@import "reset.css"
/**************************************
 *
 * ^^^^^
 * Get the Basics out of the way, import fonts and reset css
 * 
 **************************************/


/**********************************
 *
 * Font and Color global defintions
 *
 **********************************/
font-size = 16px
font = font-size "Open Sans", Helvetica, Arial, sans-serif
font2 = 'Luckiest Guy'

very-dark = #2d6031
dark = #000
semi-light = #aaa
very-light = #fff
light = #FFF
very-dark-alt = #4e7a3c
semi-dark = #6f6f6e
mostly-dark = #333
red = #FCC
green = #CFC
yellow = #FFC
darkShift = 60
boxbg = rgba(190,190,190,.1)
gray = #DBDBDB


alt-color = #0af

attention = #359b34

/********************************
 *
 * Generic Functions for css3/etc
 *
 ********************************/
alpha-bg(color,n)
  background rgb(color);
  background rgba(color,n);
box-shadow(n = 4px, color = semi-light, b = n)
  -moz-box-shadow: n n b color
  -webkit-box-shadow: n n b color
  box-shadow: n n b color

border-radius(n)
  -webkit-border-radius n
  -moz-border-radius n
  border-radius n
border-radius-except-top-right(n)
  -webkit-border-radius n
  -moz-border-radius n
  border-radius n
  border-top-right-radius 0
  -moz-border-radius-topright 0
  -webkit-border-top-right-radius 0
border-radius-top-only(n)
  border-top-left-radius n
  -moz-border-radius-topleft n
  -webkit-border-top-left-radius n
  border-top-right-radius n
  -moz-border-radius-topright n
  -webkit-border-top-right-radius n
opacity(n)
  opacity n
  filter alpha\(opacity\=n*100\)
absolute()
  display none
  position absolute
  top 0
  left 0
gradient(color1=#444444,color2=#999999)
  background-color color1
  background-image -webkit-gradient(linear, left top, left bottom, from(color1), to(color2)) /* Saf4+, Chrome */
  background-image -webkit-linear-gradient(top, color1, color2) /* Chrome 10+, Saf5.1+, iOS 5+ */
  background-image -moz-linear-gradient(top, color1, color2) /* FF3.6 */
  background-image -ms-linear-gradient(top, color1, color2) /* IE10 */
  background-image -o-linear-gradient(top, color1, color2) /* Opera 11.10+ */
  background-image linear-gradient(top, color1, color2)
  filter unquote('progid:DXImageTransform.Microsoft.gradient(startColorStr=color1, EndColorStr=color2)') /* IE6–IE9 */
submit()
  cursor pointer
  text-decoration none
  padding 8px 20px 0 20px
  gradient(lighten(very-dark-alt,20),very-dark-alt)
  box-shadow(2px, dark,4px)
  border none
  font-family font2
  font-size 24px
  border-radius(15px)
  margin 0 6px 6px 0
  color light

/**************************************
 *
 * Top level "tag" definitions, e.g. h1
 *
 **************************************/
body
  font font
  background mostly-dark
  color very-light
  h1,h2,h3
    font-family font2
    text-transform uppercase
    font-weight normal
  input,select
    font font
  b
    font-weight bold
  a
    color light
  ul
    overflow hidden
    li
      float left
      display block

  /**************************************
   *
   * The grey fade stripe below the header 
   *
   **************************************/
  .bg2
    absolute()
    width 100%
    margin-top 108px
    height 70px
    background semi-dark
    display block
    z-index -1

  /**************************************
   *
   * The header at the top of every page
   *
   **************************************/
  .header
    background gray
    color very-dark
    a
      color very-dark-alt
    .container
      padding 10px 0
      overflow hidden
      .logo
        background url('/images/KBHeader.png') no-repeat 0 15px
        height 100px
        width 320px
        float left
        cursor pointer
      .sign-up
        width auto
        text-align center
        .disclaimer
          color semi-dark
          font-size 14px
          margin 0 0 10px 0
        float right
        .button
          display block
          padding 20px 30px 5px 30px
          gradient(lighten(attention,20),attention)
          box-shadow(4px, darken(attention,20),8px)
          margin 0 8px 8px 0
          border-radius(15px)
          font-size 48px
          font-weight normal
          font-family font2
          color light
          text-decoration none
    .navigation
      .container
        padding 0
      background mostly-dark
      overflow hidden
      ul
        float right
      li
        margin 0 0 0 30px
        padding 3px 0
      font-size 14px
      a
        color light
        text-decoration none
      color semi-dark
      text-decoration none
  /************************************************
   *
   * Body top level tag definitions, e.g. p margins
   *
   ************************************************/

  h1
    font-size 2em
  .headline
    .container
      padding 20px 0 10px 0
    background mostly-dark
  .body
    gradient(gray,darken(gray,15))
    //background very-dark url('/images/body-bg.png') repeat-x 0 70px
    h2
      font-size 2em
      line-height 2em
      margin-top -20px
    i
      font-style italic
    p
      margin 15px 0
    .content-area
      gradient(alt-color,darken(alt-color,10))
      border-radius(8px)
      box-shadow(2px,dark,8px)
    .step-copy
      margin 15px
      color dark
      li
        font-family 'Luckiest Guy'
        height 200px
        width 200px
        font-size 36px
        line-height 42px
      .scan
        background url('/images/step1-scan.png') no-repeat 0px 50px
        width 193px
        div
          margin 10px 0 0 40px
      .earn
        background url('/images/step2-earn.png') no-repeat 70px 50px
        width 200px
        div
          margin 10px 0 0 50px
      .burger
        background url('/images/step3-burger.png') no-repeat 20px 100px
        width 140px
        div
          margin 10px 0 0 35px
    .separator
      margin 30px 15px
    .my-copy
      border 4px dashed darken(alt-color,20)
      color dark
      margin 15px
      p
        margin 15px 15px
    .col-left,.col-right
      margin 15px
      text-align center
      color dark
      .qr-container
        display none
        margin 10px
        padding 5px
        background light
        border 1px solid semi-dark
        color dark
        width auto
      .text
        display none
        margin 5px
    .col-left
      float left
    .col-right
      float right
    /**************************************
     *
     * Home Page specific containers and structure
     *
     **************************************/
    .clear
      clear both
    .container
      .content-area
        float left
        max-width 580px
      .phone-area
        margin-top -75px
        width 300px
        float right
        .screenshot
          margin-left 15px
          background url('/images/screenshot_03.png')
          height 584px
          width 300px
          .screens
            float left
            margin-left 26px
            margin-top 105px
            height 378px
            width 252px
            overflow hidden
            ul
              width 9000px
              li
                float left

    /**************************************
     *
     * Generic row and row alt styling used
     * in vendors and users long scrolling
     * pages.
     *
     **************************************/
    .row
      background semi-light
      a
        color dark
      color dark
      box-shadow(4px, light)
      li
        white-space no-wrap
        width 205px
        overflow hidden
        padding 10px
      margin 30px 0
    .row:nth-child(even)
      color semi-dark
      a
        color semi-dark
      background light
    .add-row
      margin 60px 0
      text-align center
      a
        submit()
    .modified,.modified:nth-child(even)
      background green
      box-shadow(12px, green)
    .deleted,.deleted:nth-child(even)
      background red
      box-shadow(12px, red)

  /**************************************
   *
   * Containers are for width boundaries
   * only
   *
   **************************************/
  .container
    max-width 900px
    margin 0 auto
    padding 30px
  .footer
    margin-top 30px
    text-align center
    background mostly-dark
    .other
      padding 0 0 30px 0
      color semi-dark
      font-size 12px
      a
        color semi-dark
        font-size 12px
        text-decoration none
    .container
      border-top 1px solid semi-dark
      padding 15px 0 0 0
    ul
      text-align center
      li
        display inline-block
        float inherit
        margin 15px 10px
  /**************************************
   *
   * FAQ Page
   * questions and answers
   *
   **************************************/
  .highlighted-item
    background-color very-dark
    font-family font2
    padding: 10px 10px 5px 10px
    color light
    font-size 24px
    line-height 36px
    text-align center
  .faq
    .sample 
      border 1px solid semi-light
    .set
      display block
      margin-bottom 60px
      .line
        margin 20px 0
        display block
        color dark
        overflow hidden
        h3
          font-family font2
          float left
          font-size 36px
          line-height 36px
          margin 20px
        text-align center
        img
          margin 15px
        p
          margin-left 65px
          text-align left
          font-size 13px
          line-height 36px
      .q
        border-radius(8px)
        background mostly-dark
        h3
          color very-light
        p
          color light
  /**************************************
   *
   * These are the little popups above
   * fields that say 'valid' or 'error'
   *
   **************************************/
  .tooltip
    border 1px solid darken(yellow,darkShift)
    border-radius(3px)
    height 20px
    line-height 20px
    padding 2px 8px
    background yellow
    color dark
    font-size .7em
    margin-top -29px
    absolute()
  
  /**************************************
   *
   * The modal background and window
   * used everywhere.
   *
   **************************************/
  .modal
    background dark
    opacity(.8)
    height 100%
    width 100%
    absolute()
  .close
    font-size 1em
    padding 4px 30px
    background semi-light
    alpha-bg(semi-light,.95)
    color semi-dark
    absolute()
    cursor pointer
    border-radius-top-only(8px)
  .window
    alpha-bg(light,.95)
  .body
    .window
      gradient(alt-color,darken(alt-color,10))
      border-radius(8px)
      box-shadow(2px,dark,8px)
      color dark
  .window
    border-radius-except-top-right(8px)
    color very-dark
    a
      color very-dark
    h2
      font-size 1.5em
    padding 30px
    absolute()
    overflow hidden
    .error-message
      padding 4px 8px
      border 1px solid darken(red,darkShift)
      background red
    /**************************************
     *
     * Tabs for vendors multiple forms
     *
     **************************************/
    .tabs
      margin 0 0 30px 0
      li
        border-radius-top-only(8px)
        font-family font2
        background very-dark
        padding 10px 15px 0 15px
        cursor pointer
        color semi-light
        margin-top 10px
        margin-right 10px
        line-height 24px
        font-size 16px
      li.active
        margin-top 0
        padding 15px 15px 5px 15px
        border-bottom none
        font-size 24px
        color light
        cursor auto
      border-bottom 1px solid very-dark
    .previews
      iframe
        width 100%
        height 200px
        margin-bottom 30px
    fieldset
      /**************************************
       *
       * Form labels and inputs structure
       *
       **************************************/
      .fields
        margin 15px 0
        overflow hidden
        label
          width 150px
          display block
          float left
        .input
          width 240px
          display block
          float right
          border 1px solid semi-dark
          border-radius(4px)
          padding 4px 8px
          margin 0
        .error
          border 1px solid darken(red,darkShift)
          background red
        .valid
          border 1px solid darken(green,darkShift)
          background green
        .loading
          border 1px solid darken(very-light,darkShift)
          background very-light
        .real-address
          float right
          height 40px
          width 246px
          padding 5px
          display block
          border 1px solid semi-dark
          border-bottom 0
          font-size 10px
          text-align right
        .google-map
          float right
          height 100px
          width 256px
          display block
          border 1px solid semi-dark
          border-top 0
          overflow hidden
        .disabled
          opacity(.3)
      .factual-details
        li
          float none
          display block
          font-size 10px
          text-align right
      .required
        label,.input
          font-weight bold
      /**************************************
       *
       * Rows, like for users in vendors screen
       *
       **************************************/
      .row
        margin 15px 0
        background semi-light
        box-shadow(4px, semi-light)
        padding 15px
        li
          height 2em
          line-height 2em
          overflow hidden
          padding-right 10px
          .input
            width 90px
          .buy_qty
            width 40px
          .buy_item
            width 120px
          .get_item
            width 180px
        .text
          clear both
          float inherit
          .tag_line
            width 100%
            text-align center
        .buttons
          padding 15px 0 0 0
          text-align center
          float inherit
          .print
            submit()
      .add-row
        margin 30px 0
        text-align center
        a
          submit()

    /**************************************
     *
     * Print Dialog
     *
     **************************************/
    .print
      .option
        margin 15px 0
        padding 15px
        background boxbg
        box-shadow(4px)
        .row
          margin 5px 0
          .column-one
            width 100px
        .text
          input
            width 350px
        .warning
          display none
          .alert
            width 340px
            padding 5px
            background red
            border 1px solid darken(red,darkShift)
    /**************************************
     *
     * Buttons at the bottom of most windows
     *
     **************************************/
    .buttons
      overflow hidden
      width 100%
      .submit,.cancel
        margin 30px 0 0 0
        font-size 1.2em
        border 1px solid semi-dark
        border-radius(4px)
        padding 0 30px
      .cancel
        background semi-light
        color semi-dark
      .submit
        float right
        margin-left 15px
        submit()
      .cancel
        margin-right 15px
        float left
  /**************************************
   *
   * For the login screen, the modal and
   * window within a container.
   *
   **************************************/
  .container .modal
    display none
  .container .window
    display block
    position relative
    margin 0 auto

  /**************************************
   *
   * For loading html to be cloned or 
   * shown by javascript later.
   *
   **************************************/
  .assets
    display none